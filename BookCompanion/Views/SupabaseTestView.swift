//
//  SupabaseTestView.swift
//  BookCompanion
//
//  Placeholder - not currently used in app
//
import SwiftUI

struct SupabaseTestView: View {
    var body: some View {
        VStack {
            Text("Supabase Test View")
                .font(.title)
            Text("This view is not currently active in the app")
                .font(.caption)
                .foregroundColor(.secondary)
        }
        .padding()
    }
}
